<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    >
    <data>
        <variable
            name="reporte"
            type="unicauca.movil.gegan.models.Reporte"/>
        <variable
            name="handler"
            type="unicauca.movil.gegan.adapters.ReporteAdapter"/>
    </data>

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:onClick="@{()->handler.onClickReporte(reporte.id)}"
        >
     <android.support.v7.widget.CardView
         android:layout_margin="@dimen/xsmall"
         android:layout_width="match_parent"
         android:layout_height="wrap_content"
         android:elevation="@dimen/small">
         <RelativeLayout
             android:layout_width="match_parent"
             android:layout_height="wrap_content"
             android:padding="@dimen/small">
             <TextView
                 android:id="@+id/valor"
                 android:layout_width="match_parent"
                 android:layout_height="wrap_content"
                 tools:text="Valor"
                 style="@style/Base.TextAppearance.AppCompat.Title"
                 android:textSize="@dimen/icon"
                 android:text='@{""+reporte.valor}'
                 />
             <TextView
                 android:id="@+id/tipo"
                 android:layout_below="@id/valor"
                 android:layout_width="match_parent"
                 android:layout_height="wrap_content"
                 tools:text="@string/tipo"
                 style="@style/TextAppearance.AppCompat.Body1"
                 android:textSize="@dimen/small"
                 android:text="@{reporte.tipo}"
                 />
             <ImageView
                 android:id="@+id/icon_delete"
                 android:layout_width="wrap_content"
                 android:layout_height="wrap_content"
                 android:src="@drawable/ic_delete2_24dp"
                 android:layout_alignParentRight="true"
                 android:onClick="@{()->handler.onClickDelete(reporte.id)}"

                 />
             <ImageView
                 android:id="@+id/icon_edit"
                 android:layout_width="wrap_content"
                 android:layout_height="wrap_content"
                 android:src="@drawable/ic_edit2_24dp"
                 android:layout_below="@id/icon_delete"
                 android:layout_alignParentRight="true"
                 android:onClick="@{()->handler.onClickEdit(reporte.id)}"
                 />
         </RelativeLayout>
     </android.support.v7.widget.CardView>

    </FrameLayout>
</layout>
